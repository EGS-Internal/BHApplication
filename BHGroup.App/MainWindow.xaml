<Window x:Class="BHGroup.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BHGroup.App"
        xmlns:viewModel="clr-namespace:BHGroup.App.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="920"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        >
    <Window.DataContext>
        <viewModel:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>

        <Style TargetType="{x:Type Image}"
               x:Key="NavIcon">
            <Style.Setters>
                <Setter Property="Width" Value="25"/>
                <Setter Property="Height" Value="25"/>
                <Setter Property="Margin" Value="10 0 0 0"/>
            </Style.Setters>
        </Style>
    </Window.Resources>
    <Border  Background="{Binding Theme}" CornerRadius="20">
        <Grid Name="gridMain">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock
                VerticalAlignment="Center"
                HorizontalAlignment="Left"
                Foreground="{Binding TextColor}"
                FontSize="22"
                Margin="20 0 0 0"
                Text="Bro is fkup">
            </TextBlock>
            <StackPanel Grid.Row="1">
                <StackPanel Orientation="Horizontal">
                    <Image Style="{StaticResource NavIcon}" 
                           Source="./Public/Image/baseline_home_black_24dp.png" />
                    <RadioButton Content="Home"
                                 Foreground="{Binding TextColor}"
                                 Style="{Binding MyButtonStyle}"
                                 Command="{Binding HomeCommand}"/>  
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Image Style="{StaticResource NavIcon}" 
                           Source="./Public/Image/baseline_person_black_24dp.png" />
                    <RadioButton Content="Stutdents"
                                 Foreground="{Binding TextColor}"
                                 Style="{Binding MyButtonStyle}"
                                 Command="{Binding StudentCommand}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Image Style="{StaticResource NavIcon}" 
                           Source="./Public/Image/teacher_2354280.png" />
                    <RadioButton Content="Lecturers"
                                 Foreground="{Binding TextColor}"
                                 Style="{Binding MyButtonStyle}"
                                 Command="{Binding LecturerCommand}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Image Style="{StaticResource NavIcon}" 
                           Source="./Public/Image/exam_10402786.png"/>
                    <RadioButton Content="Coures"
                                 Foreground="{Binding TextColor}"
                                 Style="{Binding MyButtonStyle}"
                                 Command="{Binding CourseCommand}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Image Style="{StaticResource NavIcon}" 
                           Source="./Public/Image/presentation.png"/>
                    <RadioButton Content="Classes"
                                 Foreground="{Binding TextColor}"
                                 Style="{Binding MyButtonStyle}"
                                 Command="{Binding ClassCommand}"/>  
                </StackPanel>
            </StackPanel>
            <TextBox Width="250"
                     Height="40"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Left"
                     Margin="5"
                     Grid.Column="1"
                     Style="{StaticResource TextBoxTheme}">
                
            </TextBox>
            <Button Command="{Binding ThemeCommand}" HorizontalAlignment="Right" Width="30" Height="30" Margin="0 0 100 0" Grid.Column="1">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                            <Image Source="{Binding ModeButton}" />
                        </Grid>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Click="Button_Close_Click" HorizontalAlignment="Right" Width="20" Height="20" Margin="0 0 10 0" Grid.Column="1">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="red" CornerRadius="40">
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button HorizontalAlignment="Right" Width="20" Height="20" Margin="0 0 40 0" Grid.Column="1">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Yellow" CornerRadius="40">
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button HorizontalAlignment="Right" Width="20" Height="20" Margin="0 0 70 0" Grid.Column="1">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="green" CornerRadius="40">
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <ContentControl Grid.Row="1" Grid.Column="1" Margin="10"
                            Content="{Binding CurrentView}">
            </ContentControl>
        </Grid>
    </Border>
</Window>
